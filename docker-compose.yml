version: "3"
services:
    frontend:
        container_name: carimus-app
        build:
            context: ./carimus-app
            dockerfile: Dockerfile
        ports:
            - "3000:3000"

    backend:
        container_name: server
        build:
            context: ./server
            dockerfile: Dockerfile
        ports:
            - "5000:5000"
